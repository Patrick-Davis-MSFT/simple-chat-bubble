{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "location": {
      "value": "${AZURE_LOCATION}"
    },
    "appServiceSkuName": {
      "value": "${APP_SERVICE_SKU_NAME}"
    },
    "aiHordeApiKey": {
      "value": "${AIHORDE_API_KEY}"
    },
    "weatherLatLngApiKey": {
      "value": "${LATLNG_API_KEY}"
    }
  }
}
